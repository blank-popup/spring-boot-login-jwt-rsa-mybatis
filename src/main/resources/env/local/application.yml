server:
  port: 18010
  servlet:
    contextPath: /local

spring:
  datasource:
    url: jdbc:log4jdbc:mariadb://192.168.200.159:18020/davebase?characterEncoding=UTF-8&serverTimezone=Asia/Seoul&allowMultiQueries=true
#    url: jdbc:log4jdbc:mariadb://192.168.45.67:18020/davebase?characterEncoding=UTF-8&serverTimezone=Asia/Seoul&allowMultiQueries=true
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
    username: base
    password: "0000"

jwt:
  secret: ImPossible$$
  token-validity-ms: 3600000

apikey:
  secret: ImPossible$$$$$$ImPossible$$$$$$
  iv: ImPossible$$$$$$
  delimiter: $$$

directory:
  user:
    image: D:/SPRING_DATA/user/image

logging:
  pattern:
    file: "[%d{yyyy-MM-dd HH:mm:ss.SSS}][%-5level][%logger.%method:%line] - %msg%n"
    rolling-file-name: "D:/WorkSpace/log/test/loginRSA-local.%d{yyyy-MM-dd}.%i.log"
    console: "[%d{yyyy-MM-dd HH:mm:ss.SSS}][%-5level][%logger.%method:%line] - %msg%n"
  file:
    name: D:/WorkSpace/log/test/loginRSA-local.log
    max-history: 30
  level:
    org:
      springframework: INFO
    com:
      example:
        loginJwtRSA: TRACE
    log4jdbc:
      log4j2: ERROR
#    jdbc:
#      sqlonly: TRACE
#      sqltiming: TRACE
#      resultsettable: TRACE
#      audit: TRACE
#      resultset: TRACE
#      connection: TRACE
